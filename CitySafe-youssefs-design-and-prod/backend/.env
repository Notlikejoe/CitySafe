# Backend Environment Configuration
PORT=4000
NODE_ENV=development

# JWT Secret — CHANGE THIS IN PRODUCTION to a long random string
JWT_SECRET=citysafe-dev-secret-change-in-prod

# Set to "true" to enable JSON file persistence across restarts
PERSIST_STORE=true
